schema: ./src/graphql/typeDefs.ts
generates:
  ./src/generated/types.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
      contextType: ../types/context.types#Context
      mappers:
        User: ../entity/User#UserDB
